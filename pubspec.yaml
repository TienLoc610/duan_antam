name: duan_antam
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0' # Đã nâng version tối thiểu để tương thích thư viện mới

dependencies:
  flutter:
    sdk: flutter

  # --- CÁC THƯ VIỆN ĐÃ CẬP NHẬT (FIX LỖI) ---
  firebase_core: ^3.0.0        # Cập nhật mới nhất để sửa lỗi Web
  cloud_firestore: ^5.0.0      # Cập nhật mới nhất
  firebase_storage: ^12.0.0    # Cập nhật mới nhất
  firebase_auth: ^5.1.0   
    # Cập nhật mới nhất

  url_launcher: ^6.2.1         # Gọi điện
  intl: ^0.19.0                # Định dạng ngày
  
  image_picker: ^1.1.0         # Chọn ảnh (Bản mới ổn định hơn)
  cached_network_image: ^3.3.1 # Hiển thị ảnh
  
  # QUAN TRỌNG: Bản 5.0.0 đã sửa lỗi xung đột tên "CarouselController"
  carousel_slider: ^5.0.0      

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Nếu bạn dùng font Arimo, hãy bỏ comment dòng dưới và đảm bảo có file font
  # fonts:
  #   - family: Arimo
  #     fonts:
  #       - asset: assets/fonts/Arimo-Regular.ttf
  #       - asset: assets/fonts/Arimo-Bold.ttf